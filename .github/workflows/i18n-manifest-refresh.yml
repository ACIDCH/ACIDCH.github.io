name: Temporary i18n manifest refresh

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  refresh-manifest:
    if: github.head_ref == 'codex/remove-placeholder-projects'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out PR head branch
        uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Rebuild bilingual manifest from current content
        run: node scripts/i18n-sync.mjs --write

      - name: Commit refreshed manifest when changed
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .i18n/manifest.json
          if git diff --cached --quiet; then
            echo "Manifest already matches current content."
            exit 0
          fi
          git commit -m "Refresh bilingual manifest after project cleanup"
          git push origin HEAD:${{ github.head_ref }}
